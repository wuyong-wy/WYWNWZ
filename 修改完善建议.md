# WYWNWZ 项目修改完善建议

> 基于 CC项目检测报告 + CX项目检测报告 合并分析 | 2026-05-27
>
> 两份报告共发现 **55+ 项问题**，去重合并后按优先级整理为以下可执行的修改方案。

---

## P0 — 必须立即修复（阻碍上线）

### 1. Celery 任务在数据库事务提交前触发（CX-B1 / CC 未直接覆盖）

- **位置**：`fastapi/app/api/inquiries.py:35-38`
- **问题**：`await db.flush()` 后立即调用 `send_inquiry_auto_reply.delay()`，但事务在 `get_db` 依赖退出后才 commit（即 HTTP 响应返回后）。Celery worker 可能在事务提交前查询数据库，导致找不到记录，**邮件静默丢失**。
- **修复**：

```python
# inquiries.py — 改为在后台任务中确保事务已提交
from fastapi import BackgroundTasks

@router.post("/", response_model=dict, status_code=201)
async def create_inquiry(
    inquiry: InquiryCreate,
    db: AsyncSession = Depends(get_db),
):
    db_inquiry = Inquiry(**inquiry.model_dump())
    db.add(db_inquiry)
    await db.flush()
    await db.refresh(db_inquiry)
    inquiry_id = str(db_inquiry.id)

    # 先 commit，确保 Celery worker 能查到记录
    await db.commit()

    # 事务提交后再触发 Celery 任务
    send_inquiry_auto_reply.delay(inquiry_id)
    send_admin_notification.delay(inquiry_id)

    return {"success": True, "inquiry_id": inquiry_id}
```

同时修改 `get_db` 依赖，避免重复 commit：

```python
# database.py
async def get_db():
    async with async_session() as session:
        try:
            yield session
            await session.commit()
        except Exception:
            await session.rollback()
            raise
        # 如果路由已经 commit，这里的 commit 是空操作（安全）
```

### 2. ADMIN_PASSWORD 生产环境无阻止级校验（CC-#2）

- **位置**：`fastapi/app/config.py:61-66`
- **问题**：密码为空或过短仅打 `warning`，不阻止启动。部署时忘记设置密码会导致空密码可登录。
- **修复**：

```python
# config.py — model_post_init 中
if not self.DEBUG:
    if not self.ADMIN_PASSWORD or len(self.ADMIN_PASSWORD) < 16:
        raise ValueError(
            "ADMIN_PASSWORD must be set with >= 16 characters in production. "
            "Set via ADMIN_PASSWORD environment variable."
        )
```

### 3. Nginx 缺失 HSTS 头 + SSL 密码套件不完整（CC-#6, #7, #8 / CX-S2）

- **位置**：`nginx/nginx.conf:67, 60-135`
- **问题**：
  - 无 `Strict-Transport-Security` 头
  - `ssl_ciphers` 未禁用 `aNULL:eNULL:MD5`
  - 缺少 `ssl_ecdh_curve`
  - 无 CSP 头
- **修复**：

```nginx
# 在 443 server 块中添加
add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;
add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: https:; connect-src 'self' https://api.yourdomain.com;" always;

# 修改 ssl_ciphers
ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:!aNULL:!eNULL:!MD5;
ssl_ecdh_curve X25519:secp384r1;
```

### 4. 全局占位域名硬编码（CX-B3）

- **位置**：8 个前端文件 + `next-sitemap.config.js`
  - `frontend/app/[locale]/layout.tsx`
  - `frontend/app/[locale]/page.tsx`
  - `frontend/app/[locale]/products/[slug]/page.tsx`
  - `frontend/app/[locale]/categories/[slug]/page.tsx`
  - `frontend/components/shared/SchemaMarkup.tsx`
  - 其他文件中的 `yourdomain.com`
- **问题**：部署前必须手动搜索替换，容易遗漏。
- **修复**：
  1. 创建 `frontend/lib/constants.ts`：

```typescript
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL || "https://yourdomain.com";
export const SITE_NAME = process.env.NEXT_PUBLIC_SITE_NAME || "Foreign Trade";
```

  2. 全局替换所有硬编码域名为 `SITE_URL` 引用。
  3. 在 `.env.example` 中添加 `NEXT_PUBLIC_SITE_URL=https://yourdomain.com`。

---

## P1 — 上线前应完成

### 5. 修复 get_db 事务管理逻辑（CC-#16）

- **位置**：`fastapi/app/database.py:27-35`
- **问题**：读操作（GET 请求）也会触发 commit，不必要且可能在异常时隐藏问题。
- **修复**：方案见 P0-#1，让路由层控制提交时机。或改用更精细的依赖：

```python
# 读依赖（不提交）
async def get_db_readonly():
    async with async_session() as session:
        yield session

# 写依赖（自动提交）
async def get_db():
    async with async_session() as session:
        try:
            yield session
            await session.commit()
        except Exception:
            await session.rollback()
            raise
```

### 6. 移除 config.py 硬编码的默认 JWT 密钥（CC-#1）

- **位置**：`fastapi/app/config.py:9`
- **问题**：`_DEFAULT_JWT_SECRET` 硬编码在源码中，开发环境会静默使用。
- **修复**：删除 `_DEFAULT_JWT_SECRET`，改为 `ADMIN_JWT_SECRET: str = ""`，`model_post_init` 中同时对 DEBUG 模式做检查：

```python
ADMIN_JWT_SECRET: str = ""

def model_post_init(self, __context) -> None:
    if not self.ADMIN_JWT_SECRET or len(self.ADMIN_JWT_SECRET) < 32:
        if not self.DEBUG:
            raise ValueError("ADMIN_JWT_SECRET must be >= 32 chars in production")
        else:
            logger.warning("ADMIN_JWT_SECRET is not set — using insecure default for development only")
            self.ADMIN_JWT_SECRET = "dev_insecure_jwt_secret_do_not_use_in_prod__"
```

### 7. Redis 密码避免命令行暴露（CC-#5）

- **位置**：`docker-compose.yml:139`, `scripts/healthcheck.sh:40`
- **修复**：

```yaml
# docker-compose.yml — redis healthcheck
healthcheck:
  test: ["CMD-SHELL", "REDISCLI_AUTH=$${REDIS_PASSWORD} redis-cli ping"]
```

```bash
# healthcheck.sh — 改用 REDISCLI_AUTH 环境变量
REDIS_PASSWORD=$(grep "^REDIS_PASSWORD=" .env 2>/dev/null | cut -d= -f2 || echo "")
docker exec -e REDISCLI_AUTH="$REDIS_PASSWORD" $(docker ps -qf name=redis) redis-cli ping
```

### 8. 健康检查端点增强（CC-#19）

- **位置**：`fastapi/app/api/health.py:8-11`
- **修复**：增加 DB 和 Redis 连通性检测：

```python
from fastapi import APIRouter
from sqlalchemy import text
from app.database import async_session
import redis.asyncio as aioredis
from app.config import settings

router = APIRouter(tags=["health"])

@router.get("/health")
async def health_check():
    checks = {"status": "ok", "database": "ok", "redis": "ok"}

    # 数据库检查
    try:
        async with async_session() as session:
            await session.execute(text("SELECT 1"))
    except Exception:
        checks["database"] = "unavailable"
        checks["status"] = "degraded"

    # Redis 检查
    try:
        r = aioredis.from_url(settings.REDIS_URL)
        await r.ping()
        await r.close()
    except Exception:
        checks["redis"] = "unavailable"
        checks["status"] = "degraded"

    return checks
```

### 9. 锁定关键依赖版本（CX-S3 / CC-#4）

- **位置**：`fastapi/requirements.txt`, `docker-compose.yml`
- **修复**：
  - `requirements.txt` 中 `fastapi==0.115.*` → `fastapi==0.115.6`
  - `docker-compose.yml` 中 `saleor-dashboard:latest` → `saleor-dashboard:3.20.3`

### 10. 前端添加 ErrorBoundary（CC-#28）

- **位置**：新建 `frontend/components/shared/ErrorBoundary.tsx`
- **修复**：

```tsx
"use client";
import { Component, type ReactNode } from "react";

interface Props { children: ReactNode; fallback?: ReactNode; }
interface State { hasError: boolean; }

export class ErrorBoundary extends Component<Props, State> {
  state: State = { hasError: false };
  static getDerivedStateFromError(): State { return { hasError: true }; }

  render() {
    if (this.state.hasError) {
      return this.props.fallback ?? (
        <div className="flex min-h-screen items-center justify-center">
          <p className="text-sm text-gray-500">Something went wrong. Please refresh the page.</p>
        </div>
      );
    }
    return this.props.children;
  }
}
```

在 `app/[locale]/layout.tsx` 中包裹页面内容。

---

## P2 — 上线后应尽快完成

### 11. Celery 任务改用同步数据库会话（CC-#17 / CX-B2）

- **位置**：`fastapi/app/tasks/email.py:58,98`
- **问题**：`asyncio.run()` 每次创建新事件循环，无法复用连接池。
- **修复**：创建同步数据库引擎和会话工厂：

```python
# database.py — 添加同步引擎
from sqlalchemy import create_engine
from sqlalchemy.orm import Session, sessionmaker

sync_engine = create_engine(
    settings.DATABASE_URL.replace("+asyncpg", "").replace("postgresql+asyncpg", "postgresql"),
    pool_pre_ping=True,
)
SyncSession = sessionmaker(sync_engine)

# tasks/email.py — 改用同步查询
def send_inquiry_auto_reply(self, inquiry_id: str):
    db = SyncSession()
    try:
        inquiry = db.query(Inquiry).filter(Inquiry.id == inquiry_id).first()
        if not inquiry:
            return
        # 同步发送邮件...
    finally:
        db.close()
```

### 12. 双数据库合并建议（CC-#29）

- **问题**：FastAPI 仅有一张 `inquiries` 表，单独占用一个 PG 实例，运维成本翻倍。
- **建议**：MVP 阶段将 `inquiries` 表迁入 Saleor 数据库的独立 schema：

```sql
CREATE SCHEMA IF NOT EXISTS fastapi;
CREATE TABLE fastapi.inquiries (...);
```

修改 `DATABASE_URL` 指向 Saleor 数据库 + `options=-c search_path=fastapi,public`。

### 13. 前端添加基础测试（CC-#37, #38 / CX 测试 3/5）

- **缺少**：前端无任何测试文件。
- **建议**：
  1. 添加 Vitest + React Testing Library
  2. 至少覆盖：`InquiryForm` 表单校验、`submitInquiry` API 调用、`Header` 导航渲染
  3. 添加 Playwright E2E 测试覆盖：首页加载 → 产品浏览 → 提交询盘 核心路径

### 14. 后端补充测试（CC-#36）

- **缺少**：
  - `app/services/email.py` — Brevo 发送测试
  - `app/services/saleor.py` — GraphQL 查询测试
  - `app/tasks/email.py` — Celery 任务测试
  - 边缘场景：超长字段、Unicode/Emoji、并发提交、SQL 注入尝试
- **建议**：Makefile test 命令添加 `--cov=app --cov-report=term --cov-fail-under=80`

### 15. Nginx Dashboard 访问控制（CC-#31）

- **位置**：`nginx/nginx.conf:90-91`
- **问题**：`allow 203.0.113.0/24` 是 RFC 5737 保留地址，永远不会匹配真实流量。
- **修复**：替换为真实的办公/运维 IP 或 VPN 网段，同时添加注释说明 `203.0.113.0/24` 是占位符。

---

## P3 — 优化建议（可延后）

| # | 内容 | 来源 |
|---|------|------|
| 16 | 创建 `.editorconfig` 文件统一缩进和换行符风格 | CX |
| 17 | 修复 `messages/zh.json` 的编码问题（Windows 下显示乱码） | CX |
| 18 | `alembic/env.py:14` 中 `from app.models import *` 改为显式导入 | CC-#43 |
| 19 | `Header` 语言切换下拉菜单添加 click-outside 关闭 | CC-#26 |
| 20 | `next.config.ts` 中 `experimental.serverActions` 移到非 experimental 配置 | CC-#27 |
| 21 | 前端 `fetch` 添加 `AbortController` 超时处理（建议 15s） | CC-#23 |
| 22 | Saleor GraphQL 客户端改为惰性初始化或 lifespan 中创建 | CC-#20 |
| 23 | `make healthcheck` 中 Redis 密码提取改用 source .env 而非 grep | CC-#15 |
| 24 | CI 部署脚本增加 `alembic upgrade --sql` dry-run 预览 | CC-#32 |
| 25 | Celery worker 增加 `task_serializer=json` + `task_send_sent_event=True` | CC-#10 |
| 26 | 前端产品数量文案用独立翻译键 `productCount` 替代字符串拼接 | CX-B4 |
| 27 | MeiliSearch 搜索在阶段 3 实现前添加功能开关标志 | CC-#34 |
| 28 | 测试数据库改用 `:memory:` 而非文件 `./test.db` | CC-#39 |
| 29 | `setup.sh` 添加 `--non-interactive` 模式 | CC-#35 |
| 30 | 后端 imports 用 isort 统一排序 | CC-#44 |

---

## 修改执行路线图

```
第 1 天 (上线阻塞)
  ├── P0-#1  Celery 事务提交顺序修复
  ├── P0-#2  ADMIN_PASSWORD 阻止级校验
  ├── P0-#3  Nginx HSTS + SSL + CSP
  └── P0-#4  全局占位域名替换为环境变量

第 2-3 天 (上线前加固)
  ├── P1-#5  数据库事务管理优化
  ├── P1-#6  JWT 密钥硬编码移除
  ├── P1-#7  Redis 密码暴露修复
  ├── P1-#8  健康检查增强
  ├── P1-#9  依赖版本锁定
  └── P1-#10 前端 ErrorBoundary

第 1 周 (上线后)
  ├── P2-#11 Celery 同步会话改造
  ├── P2-#12 双数据库合并（可选）
  ├── P2-#13 前端测试从零搭建
  ├── P2-#14 后端测试补全 + 覆盖率
  └── P2-#15 Nginx Dashboard IP 白名单

第 2 周起 (持续优化)
  └── P3 #16~#30 逐项推进
```

---

## 综合评分对比

| 维度 | CC 评分 | CX 评分 | 综合 |
|------|---------|---------|------|
| 架构设计 | 3.5 | 4 | **3.8** |
| 代码质量 | 3.5 | 4 | **3.8** |
| 安全性 | 3.0 | 4 | **3.5** |
| 测试覆盖 | 2.0 | 3 | **2.5** |
| 部署运维 | 4.0 | 3 | **3.5** |
| SEO | 4.0 | 5 | **4.5** |

**综合评分：3.6/5** — MVP 阶段可接受，完成 P0+P1 后可提升至 4.0+。
