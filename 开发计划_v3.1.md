# 中小企业外贸独立站 — 完整开发计划

> 版本：v3.1 | 日期：2026-05-26
> 技术路线：Python / Saleor / FastAPI / Next.js
> 适用对象：年营收 ¥50万~¥5000万的中小外贸企业
> 核心变更：主推方案从 WordPress 切换为 Saleor Headless（Python 技术栈）
> v3.1 修订：整合架构审查建议 — 数据隔离、部署优化、安全加固、里程碑量化

---

## 一、v3.0 路线变更说明

### 1.1 为什么从 WordPress 切换到 Saleor

| 维度 | v2.0 WordPress 路线 | v3.1 Saleor 路线 |
|------|---------------------|-------------------|
| 后端语言 | PHP（WordPress 生态） | **Python**（Django + FastAPI） |
| 架构模式 | 单体 CMS + 插件堆叠 | **Headless 前后端分离** |
| 数据所有权 | 数据库在 WP 内，导出受限 | **完整 GraphQL API，数据自由** |
| 定制化能力 | 受插件生态限制 | **完全定制，无边界** |
| 多区域支持 | WP Multisite（维护成本高） | **Saleor Channels 原生多区域** |
| 性能天花板 | 插件多后明显下降 | **API 原生，前端独立优化** |
| 技术团队 | 兼职运维即可 | **需 1-2 名 Python 开发者** |
| 长期演进 | 到一定规模必须迁移 | **架构随业务线性扩展** |

### 1.2 核心判断

**WordPress 的隐性成本被低估了。** 随着业务增长，插件冲突、性能下降、安全漏洞、数据迁移困难等问题会持续消耗精力。Saleor 的初期投入更高，但长期回报远超 WordPress：

- 插件兼容性问题 → **不存在**（无插件概念，直接写 Python）
- 性能随插件数量递减 → **不存在**（API 层独立，前端 CDN 缓存）
- 数据迁移痛苦 → **不存在**（所有数据通过 GraphQL API 可导出）
- 多语言/多区域维护噩梦 → **不存在**（Saleor Channels 原生支持）

---

## 二、核心矛盾与总体策略

### 2.1 中小企业的三个核心矛盾

| 矛盾 | 表现 | v3.1 解法 |
|------|------|-----------|
| **预算少 vs 需求多** | 想上多语言、多货币、AI 搜索，但年预算只有 5-20 万 | Saleor 开源免费，FastAPI 自建询盘系统，MeiliSearch 免费搜索 |
| **要专业 vs 没团队** | 需要一个"像样的"独立站，但没有专职前端/后端 | Saleor Dashboard 开箱即用；前端用 Next.js 模板起步；1 名 Python 全栈可驾驭 |
| **要增长 vs 缺工具** | 需要监控竞品、优化 SEO，但买不起 Ahrefs/SEMrush | 用开源免费工具（changedetection.io + RustySEO + LibreCrawl）替代商用方案 |

### 2.2 总体策略

```
阶段 0（0-2 周）: 轻量竞品分析 → 明确定位和差异化
       ↓
阶段 1（3-9 周）: Saleor MVP 上线，跑通询盘闭环
       ↓
阶段 2（3-6 月）: SEO 内容营销 + 数据驱动优化 + 自动化运营
       ↓
阶段 3（6-12 月）: 多区域扩展 + AI 能力 + 品牌进阶
```

**核心理念**：用 Python 技术栈一次性做对架构，避免后续"从 WordPress 迁移到 Headless"的痛苦。

---

## 三、竞品分析前置（阶段 0）

### 3.1 轻量分析方法（v3.1 修订：阶段 0 不部署基础设施）

> **v3.1 变更**：原计划在阶段 0 就部署 changedetection.io + RustySEO + LibreCrawl 三个工具，对中小企业而言学习成本和基础设施成本过高。改为先用零部署工具快速完成分析，持续监控工具推迟到阶段 2。

| 步骤 | 工具/方法 | 耗时 | 成本 |
|------|----------|------|------|
| 竞品技术栈检测 | Wappalyzer 浏览器插件（免费） | 10 分钟/站 | ¥0 |
| 竞品关键词分析 | Ubersuggest 免费额度（3 次/天）或 Ahrefs 免费版 | 1-2 小时 | ¥0 |
| 竞品站手动审计 | 浏览竞品站，记录产品策略/定价/UX/CTA 亮点 | 2-3 小时 | ¥0 |
| 输出差距分析 | 1 页纸《竞品差距分析》结论 | 1 小时 | ¥0 |

### 3.2 竞品分析清单

```markdown
□ 目标市场 TOP 10 竞品独立站 URL 列表
□ 每个竞品的技术栈检测（Wappalyzer 插件，10 分钟/站）
□ 每个竞品的关键词布局（Ubersuggest 分析）
□ 每个竞品的 UX/CTA/定价策略（手动记录）
□ 输出一份 1 页纸的《竞品差距分析》结论
```

### 3.3 持续监控工具（阶段 2 部署）

> 以下工具在阶段 2（已有站点需要持续监控时）再部署。

| 工具 | 用途 | 部署方式 | 耗时 |
|------|------|---------|------|
| **changedetection.io** | 监控 5-10 个竞品站的价格/新品/政策变动 | `docker run -d -p 5000:5000 dgtlmoon/changedetection.io` | 30 分钟 |
| **RustySEO** | 对竞品站做 SEO 技术审计，抓取关键词布局 | 下载编译好的二进制直接运行 | 1 小时 |
| **LibreCrawl** | 全量爬取竞品站结构，分析页面层级和内容策略 | `git clone` + `pip install` + `python app.py` | 2 小时 |

### 3.4 定期检查节奏

| 频率 | 工具 | 做什么 |
|------|------|--------|
| **每日** | changedetection.io | 查看竞品变动通知 |
| **每周** | GA4 + Clarity | 分析流量和用户行为 |
| **每两周** | RustySEO | 自己站 + 关键竞品的 SEO 审计 |
| **每月** | LibreCrawl | 全量爬取自己站，排查失效链接和 SEO 问题 |
| **每季度** | 全部工具 | 综合分析 → 调整 SEO 策略和内容计划 |

---

## 四、技术架构（v3.1 Python/Saleor）

### 4.1 整体架构蓝图

> **v3.1 变更**：前端改为 Vercel 托管（海外边缘节点最优）；FastAPI 使用独立数据库；MeiliSearch 推迟到阶段 3。

```
┌─────────────────────────────────────────────────────────────────┐
│                      Cloudflare CDN (免费套餐)                     │
│                 DNS + SSL + 缓存加速 + DDoS 防护 + WAF             │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────────────────┐    ┌───────────────────────────┐  │
│  │  前端层 (Next.js 15)     │    │      Dashboard 管理后台     │  │
│  │                          │    │                           │  │
│  │  - App Router (RSC)      │    │  - Saleor Dashboard       │  │
│  │  - Tailwind CSS 4        │    │    (React, 开箱即用)       │  │
│  │  - Shadcn/ui 组件        │    │  - 产品/订单/客户管理      │  │
│  │  - next-intl 国际化       │    │  - 多区域配置              │  │
│  │  - 托管: Vercel (边缘)   │    │  - 权限/角色管理           │  │
│  └──────────┬───────────────┘    └───────────────────────────┘  │
│             │ GraphQL API (HTTPS)                                 │
│  ┌──────────▼──────────────────────────────────────────────────┐ │
│  │                 Saleor Core (Python/Django)                   │ │
│  │                                                              │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐  │ │
│  │  │ 产品管理  │ │ 订单管理  │ │ 客户管理  │ │ Channels      │  │ │
│  │  │ Product  │ │ Checkout │ │ Account  │ │ (多区域/多币)  │  │ │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────────┘  │ │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐  │ │
│  │  │ 多语言    │ │ SEO 元数据│ │ 折扣/促销 │ │ Webhooks      │  │ │
│  │  │ Translations│ │ SEO Meta│ │ Discounts│ │ (事件驱动)    │  │ │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────────┘  │ │
│  └──────────┬───────────────────────────────────────────────────┘ │
│             │                                                     │
│  ┌──────────▼──────────────────────────────────────────────────┐ │
│  │              自定义服务层 (FastAPI)                            │ │
│  │            [独立数据库 fastapi_db，与 Saleor 数据隔离]         │ │
│  │                                                              │ │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────────┐    │ │
│  │  │ 询盘管理 API  │ │ RFQ 报价引擎  │ │ 邮件/通知服务    │    │ │
│  │  │ - 表单提交    │ │ - 自动计算    │ │ - Brevo 集成      │    │ │
│  │  │ - CRM 记录    │ │ - PDF 生成    │ │ - WhatsApp API   │    │ │
│  │  │ - 自动回复    │ │ - 人工审核    │ │ - 消息队列(Celery)│    │ │
│  │  └──────────────┘ └──────────────┘ └──────────────────┘    │ │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────────┐    │ │
│  │  │ 竞品监控服务  │ │ AI 服务       │ │ 数据分析 API     │    │ │
│  │  │ - 定时爬取    │ │ - 语义搜索    │ │ - 仪表盘数据    │    │ │
│  │  │ - 变动通知    │ │ - AI 客服     │ │ - 转化漏斗       │    │ │
│  │  │ - 报告生成    │ │ - 内容生成    │ │ - 自定义报表    │    │ │
│  │  └──────────────┘ └──────────────┘ └──────────────────┘    │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │                     数据层                                    │ │
│  │  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌──────────┐ │ │
│  │  │ PostgreSQL │ │ PostgreSQL │ │ Redis      │ │ Celery   │ │ │
│  │  │ (saleor_db)│ │(fastapi_db)│ │ (缓存/会话) │ │ (任务队列)│ │ │
│  │  └────────────┘ └────────────┘ └────────────┘ └──────────┘ │ │
│  │  ┌────────────┐ ┌──────────┐ ┌────────────────────────────┐ │ │
│  │  │ MinIO/S3   │ │ Nginx    │ │ Docker + Docker Compose    │ │ │
│  │  │ (文件存储)  │ │ (反向代理)│ │ (容器化部署)               │ │ │
│  │  └────────────┘ └──────────┘ └────────────────────────────┘ │ │
│  └─────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────┘
```

### 4.2 技术栈明细

| 层级 | 技术 | 版本 | 选型理由 |
|------|------|------|---------|
| **后端引擎** | Saleor | **3.20.3**（锁定） | Python/Django 生态，GraphQL API，开源免费，Channels 原生多区域 |
| **自定义后端** | FastAPI | 0.115+ | 高性能异步 Python，询盘/RFQ/AI 等自定义逻辑 |
| **前端** | Next.js | 15 | App Router + RSC，React 19，**Vercel 边缘部署** |
| **CSS** | Tailwind CSS | 4 | 原子化 CSS，与 Saleor Dashboard 风格一致 |
| **UI 组件** | Shadcn/ui | latest | 可定制、无依赖、与 Tailwind 深度集成 |
| **国际化** | next-intl | 4+ | Next.js App Router 原生 i18n 支持 |
| **数据库(Saleor)** | PostgreSQL | 16 | Saleor 原生支持，JSONB 灵活存储 |
| **数据库(FastAPI)** | PostgreSQL | 16 | **独立数据库**，与 Saleor 数据隔离，避免 migrations 冲突 |
| **缓存** | Redis | 7 | 会话、缓存、Celery 消息队列 |
| **搜索** | MeiliSearch | 1.12+ | **阶段 3 引入**，开源、多语言友好、自部署零成本 |
| **任务队列** | Celery | 5.4+ | Python 异步任务标准方案，**独立配置不加载 FastAPI 路由** |
| **文件存储** | MinIO / S3 | latest | 兼容 S3 API，可自部署或云服务 |
| **容器化** | Docker + Compose | latest | 一键部署，运维简单 |
| **反向代理** | Nginx | 1.26+ | 静态文件 + SSL 终结 + 负载均衡 + **Dashboard IP 白名单** |
| **CDN** | Cloudflare | Free | 全球加速 + DDoS + SSL |
| **监控** | Sentry | latest | 错误追踪，免费层够用 |
| **CI/CD** | GitHub Actions | - | 前端 Vercel 自动部署；后端 SSH 部署 |

### 4.3 为什么 Saleor 是 Python 生态最佳选择

| 对比项 | Saleor | Saleor 替代品 | 结论 |
|--------|--------|--------------|------|
| **开源协议** | BSD-3-Clause（商业友好） | Medusa (MIT)、Spree (BSD) | Saleor 最宽松 |
| **后端语言** | Python/Django | Medusa (Node.js)、Spree (Ruby) | Python 团队首选 |
| **API 类型** | GraphQL（原生） | REST (Spree) | GraphQL 前端灵活性最高 |
| **多区域** | Channels 原生支持 | 需自建或插件 | Saleor 最强 |
| **Admin UI** | Saleor Dashboard（React） | 各有实现 | Saleor Dashboard 最成熟 |
| **社区活跃度** | 20k+ GitHub Stars | Medusa 24k+, Spree 13k+ | 第一梯队 |
| **企业案例** | Lush、MAAP、Pure占了 | Medusa: edubily | 商业验证充分 |
| **文档质量** | 完整英文文档 + 教程 | Medusa 文档更好 | Saleor 文档够用 |
| **部署难度** | Docker 一键部署 | 类似 | 相当 |
| **中文支持** | 需自行翻译前端 | 同 | 均需适配 |

### 4.4 v3.1 架构关键决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 前端部署方式 | **Vercel 托管** | 海外边缘节点 SSR 性能最优；自动 preview deploy；减少自建运维 |
| FastAPI 数据库 | **独立 fastapi_db** | 与 Saleor migrations 隔离；两服务可独立升级；需关联 Saleor 数据时通过 GraphQL API 查询 |
| Celery 配置 | **独立 celery_config.py** | Worker 不加载 FastAPI 路由和中间件，减少内存占用和启动时间 |
| MeiliSearch | **阶段 3 引入** | MVP 50-100 产品用 Saleor GraphQL filter + PostgreSQL 全文搜索足够 |
| Saleor 版本 | **锁定 3.20.3** | 避免小版本升级导致不可控 breaking change；升级前 staging 验证 |

---

## 五、功能规划（三层优先级）

### 5.1 第一层：MVP 核心必备（阶段 1，第 3-9 周）

| # | 功能 | 关键要素 | 实现方案 | 优先级 |
|---|------|---------|---------|--------|
| 1 | **产品目录** | 50-100 产品，高清图 ≥5 张/产品，规格参数表，MOQ/认证信息 | Saleor Product + Attribute 自定义 | P0 |
| 2 | **双语言** | 英文 + 目标市场语言，子目录结构 `/en/` `/zh/`，hreflang | Saleor Translations + next-intl | P0 |
| 3 | **询盘表单** | 产品页嵌入询盘按钮 → 表单（产品型号/采购量/交货期）→ 自动回复 | FastAPI 询盘 API + Celery 邮件任务 | P0 |
| 4 | **WhatsApp 悬浮** | 每个页面右下角 WhatsApp 按钮 | 前端组件 + WhatsApp Business API | P0 |
| 5 | **基础 SEO** | TDK 自定义、XML Sitemap、Schema 结构化数据、Canonical URL | Saleor SEO Meta + next-sitemap | P0 |
| 6 | **移动端适配** | 响应式，移动端优先 | Tailwind CSS 响应式 + next/image | P0 |
| 7 | **关于 + 联系** | 公司介绍、资质展示、联系方式 | Next.js 静态页面 | P0 |
| 8 | **速度优化** | 图片 WebP/AVIF + CDN + ISR 缓存，PageSpeed > 85 | Cloudflare + next/image + Vercel CDN | P0 |
| 9 | **数据统计** | 流量来源、页面浏览、转化漏斗、热力图 | GA4 + Microsoft Clarity（免费） | P0 |
| 10 | **隐私合规** | Cookie 同意横幅 + 隐私政策页 + GDPR 数据删除接口 | 自建组件 + FastAPI `DELETE /inquiries/{id}` | P0 |

### 5.2 第二层：增长加速（阶段 2，第 3-6 月）

| # | 功能 | 关键要素 | 实现方案 | 优先级 |
|---|------|---------|---------|--------|
| 11 | **多语言扩展** | 根据流量数据增加第 3-4 语言 | Saleor Translations + next-intl | P1 |
| 12 | **多货币展示** | 美元 + 欧元 + 目标市场货币，实时汇率 | Saleor Channels + Open Exchange Rates | P1 |
| 13 | **博客/资源中心** | 行业文章、产品指南、FAQ、案例页 | Saleor Page Types + Next.js ISR | P1 |
| 14 | **弃单挽回** | 询盘未完成 → 24h 自动邮件提醒 | FastAPI + Celery Beat 定时任务 + Brevo | P2 |
| 15 | **邮件营销** | 新访客欢迎序列 + 定期 Newsletter | Brevo API + Celery 自动化 | P1 |
| 16 | **即时通讯扩展** | WeChat / LINE / Telegram（根据目标市场） | 各平台 Business API 前端组件 | P2 |
| 17 | **RFQ 报价草稿** | 询盘 → 自动生成报价模板 → 人工审核发送 | FastAPI + Playwright PDF 生成 | P2 |
| 18 | **SEO 内容矩阵** | 产品页 + 分类页 + 博客页三层关键词覆盖 | Saleor SEO + RustySEO 分析 | P1 |
| 19 | **竞品持续监控** | 竞品价格/新品/策略变动自动通知 | changedetection.io + FastAPI 通知 | P1 |
| 20 | **社交证明** | 客户评价墙 + 案例展示 + 认证资质 | Saleor 自定义 Page Type | P2 |
| 21 | **Google Shopping** | 产品 Feed → Google Merchant Center | FastAPI 定时生成 XML Feed | P2 |
| 22 | **转化率优化** | 基于 Clarity 热力图调整布局和 CTA | 迭代优化 | P1 |

### 5.3 第三层：品牌进阶（阶段 3，第 6-12 月）

| # | 功能 | 关键要素 | 实现方案 | 优先级 |
|---|------|---------|---------|--------|
| 23 | **AI 语义搜索** | 多语言自然语言搜索 + 容错 + 分面过滤 | MeiliSearch（此时引入）+ AI 重排序 | P3 |
| 24 | **3D 产品展示** | 核心产品 WebGL 3D 旋转 + AR 预览 | Three.js / model-viewer | P3 |
| 25 | **AI 客服机器人** | 7×24 多语言自动应答，引导询盘 | FastAPI + DeepSeek/OpenAI + RAG | P3 |
| 26 | **会员分级定价** | 不同级别客户不同定价 | Saleor User + Permission Groups | P3 |
| 27 | **多区域站点** | 多个主力市场拆分为独立 Channel | Saleor Channels（原生支持） | P3 |
| 28 | **个性化推荐** | "你可能还喜欢""配套产品推荐" | FastAPI 推荐服务 + 行为追踪 | P3 |
| 29 | **SEO 审计自动化** | 定期自动扫描站点 SEO 问题 | LibreCrawl + RustySEO + Celery Beat | P3 |
| 30 | **数据仪表盘** | 整合流量+询盘+订单+竞品数据 | Google Looker Studio（免费）+ FastAPI 数据 API | P2 |

---

## 六、多语言与 SEO 技术规范

### 6.1 多语言 URL 策略

```
推荐：example.com/en/  example.com/es/  example.com/ar/
避免：en.example.com（子域名，SEO 权重分散）
避免：example.com?lang=en（URL 参数，不利于索引）
```

### 6.2 Saleor 多语言实现

```python
# Saleor 内置 Translation 模型，支持产品/分类/属性/页面等所有实体的多语言翻译
# 前端通过 GraphQL 查询时传入 language_code 即可获取对应语言内容

query ProductBySlug($slug: String!, $lang: LanguageCodeEnum!) {
  product(slug: $slug, channel: "default") {
    id
    name
    description
    translation(languageCode: $lang) {
      name
      description
    }
    seoTitle
    seoDescription
  }
}
```

### 6.3 语言优先级

```
第一梯队：英语（必选）
第二梯队：西班牙语（覆盖欧美+拉美）/ 阿拉伯语（中东+北非）
第三梯队：法语 / 德语 / 俄语 / 葡萄牙语
第四梯队：日语 / 韩语 / 东南亚语种
```

根据目标市场选择，**MVP 只需英文 + 1 个目标市场语言**。

### 6.4 技术 SEO 清单

| 项目 | 要求 | 实现 |
|------|------|------|
| **Schema 结构化数据** | Product、Organization、BreadcrumbList、FAQ | JSON-LD，next-seo 自动生成 |
| **Hreflang** | 所有语言版本互相关联 | next-intl + Saleor Channel 自动生成 |
| **Sitemap** | 每个语言版本独立 + 自动更新 | next-sitemap 构建时生成 |
| **Canonical** | 所有页面指定规范 URL | Next.js Metadata API |
| **robots.txt** | 屏蔽后台/API 路由 | 手动配置 |
| **Core Web Vitals** | LCP < 2.5s, INP < 200ms, CLS < 0.1 | Vercel CDN + next/image + ISR |
| **图片优化** | WebP/AVIF + 响应式尺寸 + lazy loading | next/image 自动优化 |
| **HTTPS** | 全站强制 | Cloudflare SSL |

### 6.5 内容 SEO 策略

- **产品页**：独立 TDK + 结构化数据 + FAQ 区块 + 客户评价富文本
- **分类页**：300+ 字原创描述 + 子分类链接 + 筛选条件 URL 友好化
- **博客/资源页**：行业关键词覆盖 + 长尾词布局 + 内链体系
- **外链建设**：行业目录提交 + 供应商平台反向链接 + 行业协会合作

---

## 七、Sprint 实施计划（阶段 1 MVP 细化）

> **v3.1 变更**：Sprint 0 扩展为 3 周（原 2 周），增加 CI/CD 和备份机制；Sprint 编号顺延。

### Sprint 0a：基础设施（第 1 周）

| 日期 | 任务 | 产出 | 验证 |
|------|------|------|------|
| Day 1-2 | 购买域名 + 开通服务器（8C16G 或应用+DB 拆分） | 服务器就绪 | SSH 连接成功 |
| Day 2-3 | Docker + Docker Compose 环境 | 容器运行时就绪 | `docker compose ps` 正常 |
| Day 3-5 | 部署 PostgreSQL (saleor_db + fastapi_db 双数据库) + Redis | 数据层就绪 | 两个 DB 均可连接 |
| Day 5-7 | 配置 Cloudflare DNS + SSL + CDN | 全站 HTTPS | Cloudflare 显示 Active |

### Sprint 0b：Saleor + 前端骨架（第 2-3 周）

| 日期 | 任务 | 产出 | 验证 |
|------|------|------|------|
| Day 8-11 | 部署 Saleor Core **3.20.3** + Saleor Dashboard | Saleor 后端运行 | GraphQL Playground 可访问 |
| Day 11-13 | 初始化 Next.js 项目 + Tailwind + Shadcn/ui + next-intl | 前端骨架就绪 | `npm run dev` 正常启动 |
| Day 13-15 | Vercel 项目创建 + 环境变量 + 自动部署 | 前端 CI/CD 就绪 | push 后 Vercel 自动部署 |
| Day 15-17 | Saleor Channel 配置 + 双语言设置 `/en/` `/zh/` | 多语言框架就绪 | URL 切换后 Saleor API 返回正确语言 |
| Day 17-19 | GitHub Actions CI/CD（后端 lint + test + deploy） | 后端 CI/CD 就绪 | push 后自动部署到服务器 |
| Day 19-21 | 数据库备份脚本 + cron + 异地 OSS 上传 | 备份机制就绪 | 模拟恢复备份成功 |

### Sprint 1：核心功能（第 4-5 周）

| 日期 | 任务 | 产出 | 验证 |
|------|------|------|------|
| Day 22-24 | 导入 20 个产品（名称/描述/图片/属性/SEO） | 产品目录可浏览 | Saleor Dashboard 可见产品 |
| Day 24-26 | 产品列表页 + 产品详情页前端 | 前端产品展示 | 产品页展示完整（图/参数/SEO） |
| Day 26-28 | FastAPI 询盘 API（提交+存储+自动回复邮件）**独立 fastapi_db** | 询盘可收发 | POST /api/inquiries → 收到邮件 |
| Day 28-30 | 产品页嵌入询盘按钮 + 表单组件 | 询盘前端打通 | 提交测试询盘成功 |
| Day 30-32 | WhatsApp 悬浮按钮组件 | 即时通讯可用 | 点击按钮跳转 WhatsApp |
| Day 32-35 | 关于我们 / 联系我们 / 隐私政策静态页 | 基础页面就绪 | 各页面内容完整、移动端正常 |

### Sprint 2：内容 + SEO（第 6-7 周）

| 日期 | 任务 | 产出 | 验证 |
|------|------|------|------|
| Day 36-40 | 补充至 50+ 产品 + 产品分类完善 | 产品目录完整 | 分类导航清晰、字段完整 |
| Day 40-44 | 首页设计 + 分类页设计 | 核心页面完成 | Clarity 热力图无死区 |
| Day 44-46 | SEO TDK + Schema + Sitemap + hreflang | SEO 基础设施就绪 | GSC 提交 Sitemap 无报错 |
| Day 46-49 | 10 篇行业文章 + 3 个案例页 | 内容基础建立 | Lighthouse SEO 评分 > 90 |

### Sprint 3：测试与上线（第 8-9 周）

| 日期 | 任务 | 产出 | 验证 |
|------|------|------|------|
| Day 50-54 | 多设备/多浏览器/多语言全面测试 | Bug 清零 | 测试清单全部通过 |
| Day 54-56 | 性能优化（图片/缓存/ISR/CDN），PageSpeed > 85 | 性能达标 | Lighthouse Mobile > 85 |
| Day 56-58 | 安全加固：Dashboard IP 白名单 + 速率限制 + Sentry | 安全就绪 | 未授权 IP 无法访问 Dashboard |
| Day 58-60 | Google Merchant Center + GA4 + Clarity 接入 | 数据工具接入 | 各平台有数据流入 |
| Day 60-63 | 上线前终审 + GDPR 合规检查 + 正式发布 | **站点 LIVE** | GSC 索引 + GA4 数据正常 |

**阶段 1 完成标准（量化）：**

| 指标 | 目标值 |
|------|--------|
| GSC 索引页面 | > 50 |
| PageSpeed Mobile | > 85 |
| 询盘表单 E2E | 测试通过（提交→邮件→Dashboard 可见） |
| 核心产品页 SEO 评分 | > 90（至少 5 个页面） |
| GA4 数据 | 有流量记录 |
| Clarity 热力图 | 正常录制 |
| Saleor Dashboard | 可管理产品和内容 |
| 备份恢复 | 模拟恢复成功 |

---

## 八、阶段二/三实施概要

### 阶段二：增长优化（第 3-6 月）

**目标**：自然搜索流量占比 > 20%，月均询盘 ≥ 15，邮件打开率 > 25%

| 任务 | 产出 | 技术实现 |
|------|------|---------|
| 部署竞品监控工具（changedetection.io + RustySEO + LibreCrawl） | 竞品持续监控 | Docker 部署 + FastAPI 通知 |
| 根据流量数据增加第 3-4 语言 | 多语言站点扩展 | Saleor Translations + next-intl |
| 多货币展示（USD + EUR + 目标市场货币） | 本地化价格 | Saleor Channels |
| 博客/资源中心持续输出（≥2 篇/周） | SEO 内容矩阵 | Saleor Page Types |
| Brevo 邮件营销自动化（double opt-in） | 自动化运营 | FastAPI + Celery + Brevo |
| RFQ 报价草稿系统 | 询盘→报价工作流 | FastAPI + Playwright PDF |
| Google Shopping Feed | 免费购物广告位 | FastAPI 定时 XML 生成 |
| Clarity 分析 → 转化率优化 | CRO 迭代 | 数据分析 + 前端迭代 |

### 阶段三：品牌进阶（第 6-12 月）

**目标**：自然搜索流量占比 > 40%，月均询盘 ≥ 50，4+ 语言

| 任务 | 产出 | 技术实现 |
|------|------|---------|
| MeiliSearch 部署 + AI 语义搜索 | 多语言智能搜索 | MeiliSearch + AI 重排序 |
| 核心产品 3D 展示 | 差异化产品体验 | Three.js / model-viewer |
| AI 客服机器人 | 7×24 自动化客服 | FastAPI + DeepSeek API + RAG |
| 会员分级定价 | B2B 客户分层 | Saleor Permission Groups |
| 多区域独立 Channel | 多站点运营 | Saleor Channels 扩增 |
| Google Looker Studio 仪表盘 | 数据驱动决策 | FastAPI 数据 API + Looker Studio |

---

## 九、Docker Compose 生产部署配置

### 9.1 完整的 `docker-compose.yml`

> **v3.1 变更**：FastAPI 使用独立数据库；移除 nextjs 容器（Vercel 托管）；MeiliSearch 标注为阶段 3；Celery 配置独立。

```yaml
version: "3.8"

services:
  # === Saleor Core ===
  saleor-api:
    image: ghcr.io/saleor/saleor:3.20.3
    restart: always
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - .env
    depends_on:
      db-saleor:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - saleor-media:/app/media

  # === Saleor Dashboard ===
  saleor-dashboard:
    image: ghcr.io/saleor/saleor-dashboard:latest
    restart: always
    ports:
      - "127.0.0.1:9000:80"
    environment:
      - API_URL=https://api.yourdomain.com/graphql/

  # === FastAPI 自定义服务 ===
  fastapi:
    build: ./fastapi
    restart: always
    ports:
      - "127.0.0.1:8001:8001"
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql://fastapi:${FASTAPI_DB_PASSWORD}@db-fastapi:5432/fastapi
    depends_on:
      db-fastapi:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./fastapi:/app

  # === Celery Worker（独立配置，不加载 FastAPI 路由）===
  celery-worker:
    build: ./fastapi
    restart: always
    command: celery -A celery_config worker -l info -Q default,email,ai
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql://fastapi:${FASTAPI_DB_PASSWORD}@db-fastapi:5432/fastapi
    depends_on:
      db-fastapi:
        condition: service_healthy
      redis:
        condition: service_healthy

  # === Celery Beat ===
  celery-beat:
    build: ./fastapi
    restart: always
    command: celery -A celery_config beat -l info
    env_file:
      - .env
    depends_on:
      redis:
        condition: service_healthy

  # === PostgreSQL (Saleor) ===
  db-saleor:
    image: postgres:16-alpine
    restart: always
    ports:
      - "127.0.0.1:5432:5432"
    environment:
      - POSTGRES_USER=saleor
      - POSTGRES_PASSWORD=${SALEOR_DB_PASSWORD}
      - POSTGRES_DB=saleor
    volumes:
      - saleor-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U saleor"]
      interval: 5s
      timeout: 5s
      retries: 5

  # === PostgreSQL (FastAPI) — 独立数据库 ===
  db-fastapi:
    image: postgres:16-alpine
    restart: always
    ports:
      - "127.0.0.1:5433:5432"
    environment:
      - POSTGRES_USER=fastapi
      - POSTGRES_PASSWORD=${FASTAPI_DB_PASSWORD}
      - POSTGRES_DB=fastapi
    volumes:
      - fastapi-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fastapi"]
      interval: 5s
      timeout: 5s
      retries: 5

  # === Redis ===
  redis:
    image: redis:7-alpine
    restart: always
    ports:
      - "127.0.0.1:6379:6379"
    command: redis-server --requirepass ${REDIS_PASSWORD}
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  # === MeiliSearch（阶段 3 引入，MVP 阶段注释掉）===
  # meilisearch:
  #   image: getmeili/meilisearch:v1.12
  #   restart: always
  #   ports:
  #     - "127.0.0.1:7700:7700"
  #   environment:
  #     - MEILI_MASTER_KEY=${MEILI_MASTER_KEY}
  #     - MEILI_ENV=production
  #   volumes:
  #     - meili-data:/meili_data

  # === Nginx ===
  nginx:
    image: nginx:alpine
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/ssl:/etc/nginx/ssl
    depends_on:
      - saleor-api
      - fastapi

volumes:
  saleor-media:
  saleor-db-data:
  fastapi-db-data:
  redis-data:
  # meili-data:
```

### 9.2 Nginx 路由配置（含 Dashboard IP 白名单）

> **v3.1 变更**：增加 Dashboard IP 白名单限制；所有端口绑定 127.0.0.1。

```nginx
# /nginx/nginx.conf
upstream saleor_api {
    server saleor-api:8000;
}

upstream fastapi {
    server fastapi:8001;
}

server {
    listen 443 ssl http2;
    server_name api.yourdomain.com;

    # Saleor GraphQL API
    location /graphql/ {
        proxy_pass http://saleor_api;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Saleor Dashboard — IP 白名单限制
    location /dashboard/ {
        allow 203.0.113.0/24;   # 办公室 IP（替换为实际 IP）
        allow 10.0.0.0/8;       # VPN 网段（如有）
        deny all;
        proxy_pass http://saleor-dashboard/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # FastAPI 自定义 API
    location /fastapi/ {
        proxy_pass http://fastapi/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Saleor 静态媒体文件
    location /media/ {
        proxy_pass http://saleor_api;
    }
}

# 前端由 Vercel 托管，Nginx 不代理 Next.js
# 如需自建前端，取消以下注释：
# upstream nextjs { server nextjs:3000; }
# server {
#     listen 443 ssl http2;
#     server_name yourdomain.com;
#     location / { proxy_pass http://nextjs; ... }
# }
```

---

## 十、FastAPI 询盘系统设计

### 10.1 数据模型

```python
# fastapi/models/inquiry.py
from sqlalchemy import Column, String, Text, DateTime, Enum, Boolean
from sqlalchemy.dialects.postgresql import UUID
import uuid
import enum
from datetime import datetime, timezone
from app.database import Base


class InquiryStatus(str, enum.Enum):
    NEW = "new"
    REPLIED = "replied"
    QUOTED = "quoted"
    CONVERTED = "converted"
    CLOSED = "closed"


class Inquiry(Base):
    __tablename__ = "inquiries"

    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    saleor_product_id = Column(String(255), nullable=True)  # Saleor 产品 ID（引用，非外键）
    product_name = Column(String(500), nullable=True)
    customer_name = Column(String(255), nullable=False)
    customer_email = Column(String(255), nullable=False)
    customer_phone = Column(String(50), nullable=True)
    customer_company = Column(String(500), nullable=True)
    quantity = Column(String(255), nullable=True)
    delivery_deadline = Column(String(255), nullable=True)
    message = Column(Text, nullable=True)
    status = Column(Enum(InquiryStatus), default=InquiryStatus.NEW)
    language = Column(String(10), default="en")
    created_at = Column(DateTime, default=lambda: datetime.now(timezone.utc))
    replied_at = Column(DateTime, nullable=True)
    is_read = Column(Boolean, default=False)
```

> **v3.1 说明**：`saleor_product_id` 存储 Saleor 产品的 GraphQL ID 作为逻辑引用，不建立数据库外键。需要产品详情时通过 Saleor GraphQL API 查询。

### 10.2 API 端点

```python
# fastapi/api/inquiries.py
from fastapi import APIRouter, Depends, BackgroundTasks, HTTPException
from app.models.inquiry import Inquiry, InquiryStatus
from app.tasks.email import send_inquiry_auto_reply, send_admin_notification

router = APIRouter(prefix="/inquiries", tags=["inquiries"])


@router.post("/")
async def create_inquiry(
    inquiry: InquiryCreate,
    background_tasks: BackgroundTasks,
    db: Session = Depends(get_db),
):
    """提交询盘"""
    db_inquiry = Inquiry(**inquiry.model_dump())
    db.add(db_inquiry)
    db.commit()
    db.refresh(db_inquiry)

    # Celery 异步任务：发送自动回复 + 通知管理员
    send_inquiry_auto_reply.delay(str(db_inquiry.id))
    send_admin_notification.delay(str(db_inquiry.id))

    return {"success": True, "inquiry_id": str(db_inquiry.id)}


@router.get("/")
async def list_inquiries(
    status: InquiryStatus | None = None,
    page: int = 1,
    limit: int = 20,
    db: Session = Depends(get_db),
):
    """查询询盘列表（Dashboard 用）"""
    query = db.query(Inquiry).order_by(Inquiry.created_at.desc())
    if status:
        query = query.filter(Inquiry.status == status)

    total = query.count()
    items = query.offset((page - 1) * limit).limit(limit).all()

    return {"data": items, "total": total, "page": page, "limit": limit}


@router.patch("/{inquiry_id}/status")
async def update_inquiry_status(
    inquiry_id: str,
    status: InquiryStatus,
    db: Session = Depends(get_db),
):
    """更新询盘状态"""
    inquiry = db.query(Inquiry).filter(Inquiry.id == inquiry_id).first()
    if not inquiry:
        raise HTTPException(status_code=404, detail="Inquiry not found")

    inquiry.status = status
    if status == InquiryStatus.REPLIED:
        inquiry.replied_at = datetime.now(timezone.utc)
    db.commit()

    return {"success": True, "status": status}


@router.delete("/{inquiry_id}")
async def delete_inquiry(
    inquiry_id: str,
    db: Session = Depends(get_db),
):
    """删除询盘（GDPR Right to be forgotten）"""
    inquiry = db.query(Inquiry).filter(Inquiry.id == inquiry_id).first()
    if not inquiry:
        raise HTTPException(status_code=404, detail="Inquiry not found")

    db.delete(inquiry)
    db.commit()

    return {"success": True}
```

### 10.3 Celery 独立配置

> **v3.1 变更**：Celery 配置独立于 FastAPI 应用，Worker 不加载路由和中间件。

```python
# fastapi/celery_config.py
from celery import Celery
from app.config import settings

celery_app = Celery(
    "tasks",
    broker=settings.REDIS_URL,
    backend=settings.REDIS_URL,
)

celery_app.conf.update(
    task_serializer="json",
    accept_content=["json"],
    result_serializer="json",
    timezone="UTC",
    enable_utc=True,
    task_routes={
        "app.tasks.email.*": {"queue": "email"},
        "app.tasks.ai.*": {"queue": "ai"},
    },
)

# 自动发现任务模块
celery_app.autodiscover_tasks(["app.tasks"])
```

### 10.4 Celery 异步邮件任务

```python
# fastapi/tasks/email.py
from celery_config import celery_app
from app.services.email import brevo_send
from app.database import get_db_sync
from app.models.inquiry import Inquiry


@celery_app.task(queue="email")
def send_inquiry_auto_reply(inquiry_id: str):
    """自动回复询盘客户"""
    db = next(get_db_sync())
    inquiry = db.query(Inquiry).filter(Inquiry.id == inquiry_id).first()
    if not inquiry:
        return

    template = "zh" if inquiry.language == "zh" else "en"
    brevo_send(
        to_email=inquiry.customer_email,
        template_id=f"auto-reply-{template}",
        params={
            "customer_name": inquiry.customer_name,
            "product_name": inquiry.product_name or "your inquiry",
        },
    )


@celery_app.task(queue="email")
def send_admin_notification(inquiry_id: str):
    """通知管理员有新询盘"""
    db = next(get_db_sync())
    inquiry = db.query(Inquiry).filter(Inquiry.id == inquiry_id).first()
    if not inquiry:
        return

    brevo_send(
        to_email="admin@yourdomain.com",
        template_id="admin-new-inquiry",
        params={
            "inquiry_id": str(inquiry.id),
            "customer_name": inquiry.customer_name,
            "customer_email": inquiry.customer_email,
            "product_name": inquiry.product_name or "N/A",
            "message": inquiry.message or "(无留言)",
        },
    )
```

---

## 十一、CI/CD 流水线

> **v3.1 新增**：原计划缺少 CI/CD，此处补全。

### 11.1 GitHub Actions 配置

```yaml
# .github/workflows/deploy.yml
name: Deploy

on:
  push:
    branches: [main]

jobs:
  # 前端：Vercel 自动部署（无需配置，Vercel GitHub App 处理）
  # 以下为后端部署流程

  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: |
          pip install -r fastapi/requirements.txt
          pip install ruff pytest
      - name: Lint
        run: ruff check fastapi/
      - name: Test
        run: pytest fastapi/tests/ -v

  deploy-backend:
    needs: lint-and-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to server via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            cd /opt/wywmwz
            docker compose pull fastapi celery-worker celery-beat
            docker compose up -d fastapi celery-worker celery-beat
            docker compose exec fastapi alembic upgrade head
```

### 11.2 数据库备份脚本

```bash
#!/bin/bash
# scripts/backup.sh — 每日 02:00 cron 执行

BACKUP_DIR="/opt/backups"
DATE=$(date +%Y%m%d)
RETAIN_DAYS=30

# 备份 Saleor 数据库
pg_dump -h 127.0.0.1 -p 5432 -U saleor saleor | gzip > "$BACKUP_DIR/saleor_$DATE.sql.gz"

# 备份 FastAPI 数据库
pg_dump -h 127.0.0.1 -p 5433 -U fastapi fastapi | gzip > "$BACKUP_DIR/fastapi_$DATE.sql.gz"

# 上传到 OSS/S3（异地备份）
# aws s3 cp "$BACKUP_DIR/saleor_$DATE.sql.gz" s3://your-bucket/backups/
# aws s3 cp "$BACKUP_DIR/fastapi_$DATE.sql.gz" s3://your-bucket/backups/

# 清理过期备份
find "$BACKUP_DIR" -name "*.sql.gz" -mtime +$RETAIN_DAYS -delete

echo "Backup completed: $DATE"
```

```bash
# crontab -e
0 2 * * * /opt/wywmwz/scripts/backup.sh >> /var/log/backup.log 2>&1
```

---

## 十二、成本估算

### 12.1 一次性投入

| 项目 | 基础方案 | 推荐方案 | 说明 |
|------|---------|---------|------|
| 域名（年） | ¥60-100 | ¥80 | Namecheap/Cloudflare 注册 .com |
| Logo/VI | ¥0-3,000 | ¥1,000 | 可先用 Canva 自己做 |
| 产品图片处理 | ¥0-5,000 | ¥2,000 | 批量压缩、WebP 转换、去背景 |
| 前端定制开发 | ¥20,000-50,000 | ¥35,000 | Next.js 前端页面 + 组件 |
| Saleor 部署配置 | ¥8,000-20,000 | ¥12,000 | Docker + Nginx + CI/CD |
| FastAPI 询盘系统 | ¥10,000-25,000 | ¥15,000 | 询盘 API + RFQ + 邮件 |
| **一次性合计** | | **¥65,000** | ¥40,000-100,000 |

### 12.2 按团队配置的精确估算

> **v3.1 新增**：原计划区间过宽（2.5 倍差异），此处按团队配置给出更精确估算。

| 团队配置 | 前端定制 | Saleor 部署 | FastAPI 询盘 | 合计 | 周期 |
|----------|---------|------------|-------------|------|------|
| 1 名 Python 全栈（自己写前端） | ¥20K | ¥8K | ¥10K | **¥38K** | 10-12 周 |
| 1 前端 + 1 Python 后端 | ¥35K | ¥12K | ¥15K | **¥62K** | 8-9 周 |
| 外包团队 | ¥50K | ¥20K | ¥25K | **¥95K** | 6-8 周 |

### 12.3 年度运营成本

| 项目 | 月费 | 年费 | 说明 |
|------|------|------|------|
| 云服务器（8C16G 或拆分） | ¥400-800 | ¥4,800-9,600 | 应用+DB 可拆分为两台 4C8G |
| Vercel 前端托管 | ¥0 | ¥0 | Hobby 免费层够用（MVP） |
| CDN + DNS | ¥0 | ¥0 | Cloudflare 免费套餐 |
| 备份存储（OSS/S3） | ¥15-50 | ¥180-600 | 阿里云 OSS / AWS S3 |
| 邮件服务 | ¥0-50 | ¥0-600 | Brevo 免费 300 封/天 |
| SSL 证书 | ¥0 | ¥0 | Cloudflare 免费 SSL |
| 监控（Sentry） | ¥0 | ¥0 | Sentry 免费层 |
| 竞品监控 VPS（阶段 2） | ¥30-60 | ¥360-720 | changedetection.io 最小 VPS |
| AI API（DeepSeek） | ¥50-200 | ¥600-2,400 | 用于 AI 搜索 / 客服 |
| **年运营合计** | | **¥6,000-14,000** | 月均 ¥500-1,200 |

### 12.4 各阶段预算汇总

| 阶段 | 时间 | 一次性 | 累计运营 | **累计总投入** |
|------|------|--------|---------|-------------|
| 阶段 0（竞品分析） | 0-2 周 | ¥0 | ¥0 | **¥0** |
| 阶段 1（MVP） | 3-9 周 | ¥38K-95K | ¥1,000-2,500 | **¥39K-98K** |
| 阶段 2（增长） | 3-6 月 | ¥10K-30K | ¥3,000-7,000 | **¥52K-135K** |
| 阶段 3（进阶） | 6-12 月 | ¥20K-60K | ¥6,000-14,000 | **¥78K-209K** |

### 12.5 与 WordPress 方案的投入对比

| 维度 | v2.0 WordPress | v3.1 Saleor | 差异 |
|------|---------------|------------|------|
| 一次性开发 | ¥13K-20K | ¥38K-95K | +¥25K-75K |
| 年运营 | ¥1K-4.5K | ¥6K-14K | +¥5K-10K |
| 上线时间 | 2-4 周 | 8-9 周 | +4-5 周 |
| 第二年迁移成本 | 如需升级 Headless：¥200K+ | **¥0** | Saleor 无需迁移 |
| 3 年总拥有成本 | ¥16K-33K（如不需升级） | ¥84K-228K | 初期高，长期持平或更低 |

> **结论**：Saleor 路线的首年投入是 WordPress 的 2-4 倍，但避免了未来的"WordPress→Headless"迁移成本和数据丢失风险。年销预期 ¥500 万以上的企业应直接走 Saleor 路线。

---

## 十三、项目目录结构

```
wywmwz/
├── saleor/                     # Saleor Core 配置
│   ├── .env                    # 环境变量
│   └── docker-compose.yml      # 容器编排
│
├── frontend/                   # Next.js 前端（Vercel 托管）
│   ├── app/
│   │   ├── [locale]/           # 国际化路由
│   │   │   ├── layout.tsx      # 根布局
│   │   │   ├── page.tsx        # 首页
│   │   │   ├── products/
│   │   │   │   ├── page.tsx    # 产品列表
│   │   │   │   └── [slug]/
│   │   │   │       └── page.tsx # 产品详情
│   │   │   ├── categories/
│   │   │   │   └── [slug]/
│   │   │   │       └── page.tsx
│   │   │   ├── blog/
│   │   │   │   ├── page.tsx
│   │   │   │   └── [slug]/
│   │   │   │       └── page.tsx
│   │   │   ├── about/page.tsx
│   │   │   └── contact/page.tsx
│   │   └── api/               # Next.js API Routes (可选)
│   ├── components/
│   │   ├── ui/                # Shadcn/ui 组件
│   │   ├── layout/
│   │   │   ├── Header.tsx
│   │   │   ├── Footer.tsx
│   │   │   └── Navigation.tsx
│   │   ├── product/
│   │   │   ├── ProductCard.tsx
│   │   │   ├── ProductGrid.tsx
│   │   │   └── InquiryButton.tsx
│   │   ├── inquiry/
│   │   │   ├── InquiryForm.tsx
│   │   │   └── InquiryModal.tsx
│   │   └── shared/
│   │       ├── WhatsAppFloat.tsx
│   │       ├── CookieConsent.tsx
│   │       └── SEOMeta.tsx
│   ├── lib/
│   │   ├── saleor.ts          # Saleor GraphQL 客户端
│   │   ├── api.ts             # FastAPI 客户端
│   │   └── utils.ts
│   ├── graphql/
│   │   ├── queries/
│   │   │   ├── products.graphql
│   │   │   ├── categories.graphql
│   │   │   └── pages.graphql
│   │   └── fragments/
│   │       └── product.graphql
│   ├── messages/               # next-intl 翻译文件
│   │   ├── en.json
│   │   └── zh.json
│   ├── next.config.ts
│   ├── tailwind.config.ts
│   ├── tsconfig.json
│   └── package.json
│
├── fastapi/                    # FastAPI 自定义服务
│   ├── app/
│   │   ├── main.py            # FastAPI 入口
│   │   ├── config.py          # 配置
│   │   ├── database.py        # 数据库连接（fastapi_db）
│   │   ├── models/
│   │   │   ├── inquiry.py
│   │   │   └── rfq.py
│   │   ├── api/
│   │   │   ├── inquiries.py
│   │   │   ├── rfq.py
│   │   │   └── dashboard.py
│   │   ├── tasks/
│   │   │   ├── email.py
│   │   │   ├── seo.py
│   │   │   └── competitor.py
│   │   └── services/
│   │       ├── email.py       # Brevo 邮件服务
│   │       ├── pdf.py         # Playwright PDF 生成
│   │       └── ai.py          # AI 服务封装
│   ├── celery_config.py       # Celery 独立配置
│   ├── alembic/               # 数据库迁移
│   ├── tests/
│   ├── Dockerfile
│   └── requirements.txt
│
├── nginx/
│   ├── nginx.conf
│   └── ssl/
│
├── scripts/
│   ├── deploy.sh              # 部署脚本
│   ├── backup.sh              # 数据库备份（每日 cron）
│   └── seed-data.py           # 测试数据填充
│
├── .github/
│   └── workflows/
│       └── deploy.yml         # CI/CD 流水线
│
├── docker-compose.yml         # 根 Compose（引用各服务）
├── .env.example
├── .gitignore
└── README.md
```

---

## 十四、GDPR 合规清单

> **v3.1 新增**：原计划仅有 Cookie 同意横幅，此处补全 GDPR 要求。

| 要求 | 实现方案 | 阶段 |
|------|---------|------|
| Cookie 同意横幅 | react-cookie-consent 组件，分必要/分析/营销三类 | 阶段 1 |
| 隐私政策页 | Next.js 静态页，含数据处理说明 | 阶段 1 |
| 用户数据删除权 | FastAPI `DELETE /inquiries/{id}` 端点 | 阶段 1 |
| 用户数据导出权 | FastAPI `GET /inquiries/{id}/export` 返回 JSON | 阶段 2 |
| 邮件 double opt-in | Brevo 模板配置 + 确认链接 | 阶段 2 |
| 数据处理协议（DPA） | 法律文档，与 Brevo/Cloudflare 签署 | 阶段 1 |
| 服务器日志 IP 匿名化 | Nginx `anon_log_comp` + Cloudflare IP 匿名 | 阶段 1 |
| 数据保留期限 | 询盘数据 2 年后自动归档（Celery Beat 定时任务） | 阶段 2 |

---

## 十五、风险与应对

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| 上线后无询盘 | 中 | 高 | 检查：TDK 是否正确索引？CTA 是否清晰？速度 < 3s？ |
| Saleor API 性能瓶颈 | 低 | 高 | Redis 缓存热点查询；CDN 缓存产品数据；数据库索引优化 |
| Python 开发者招不到 | 中 | 中 | Saleor 基于 Django，Python Web 开发者充足；FastAPI 学习曲线低 |
| 服务器被攻击 | 低 | 极高 | Cloudflare WAF + Dashboard IP 白名单 + Fail2Ban + 定期安全审计 |
| SEO 3 个月无效果 | 中 | 中 | SEO 需 3-6 个月见效；坚持内容更新 + 外链建设；LibreCrawl 每周检查 |
| 多语言内容质量差 | 高 | 中 | 核心页面人工翻译/校对；博客用 DeepL + 人工润色 |
| 海外访问速度慢 | 中 | 高 | **Vercel 边缘节点** + Cloudflare CDN；服务器选目标市场就近区域 |
| Saleor 版本升级不兼容 | 低 | 中 | **锁定 3.20.3**；升级前在 staging 环境验证；关注 Release Notes |
| 双数据库运维复杂度增加 | 低 | 中 | 统一备份脚本；监控两个 DB 的连接数和慢查询；FastAPI DB 体量小，运维负担轻 |
| FastAPI 与 Saleor 数据不同步 | 低 | 中 | 通过 Saleor GraphQL API 实时查询产品信息；Webhook 监听产品变更事件更新缓存 |

---

## 十六、量化里程碑定义

> **v3.1 新增**：原计划阶段 1 缺少量化验收标准。

| 阶段 | 时间 | 量化标准 |
|------|------|---------|
| 阶段 0 | 第 0-2 周 | 竞品差距分析 1 页纸完成；目标市场和技术栈确定 |
| 阶段 1 | 第 3-9 周 | GSC 索引 > 50 页；PageSpeed Mobile > 85；询盘 E2E 通过；5+ 产品页 SEO > 90；GA4 有数据；备份恢复成功 |
| 阶段 2 | 第 3-6 月 | 自然搜索流量占比 > 20%；月均询盘 ≥ 15；邮件打开率 > 25%；博客 ≥ 20 篇 |
| 阶段 3 | 第 6-12 月 | 自然搜索流量占比 > 40%；月均询盘 ≥ 50；4+ 语言版本；AI 搜索使用率 > 10% |

---

## 十七、一页纸行动计划

```
┌──────────────────────────────────────────────────────────────┐
│  Week 1-2   │ 阶段 0：竞品分析（零部署工具）→ 明确定位        │
│  Week 3-5   │ 阶段 1a：基础设施 + Saleor + 前端骨架 + CI/CD  │
│  Week 5-7   │ 阶段 1b：核心功能（产品+询盘+WhatsApp+静态页） │
│  Week 7-9   │ 阶段 1c：内容+SEO+测试+安全加固 → 正式上线     │
│  Month 3-6  │ 阶段 2：SEO 内容 + 邮件营销 + 竞品监控部署      │
│  Month 6-12 │ 阶段 3：MeiliSearch + 多区域 + AI + 品牌进阶    │
└──────────────────────────────────────────────────────────────┘
```

---

## 十八、参考案例

| 案例 | 业务 | 技术栈 | 关键成果 | 启示 |
|------|------|--------|---------|------|
| **Lift Foils** | B2C+B2B 水上运动 | Next.js + Shopify Plus Headless | 300+ 网点全球覆盖 | Headless 是品牌终局 |
| **Nanobebe** | B2C 母婴 | Next.js + Storyblok + Turborepo | GTmetrix +117% | Monorepo 管理多区域 |
| **陈泽工业品** | B2B 工业品 | WordPress + WooCommerce | DA 18→34 | 内容 SEO 是 B2B 核心 |
| **edubily** | B2C 营养品 | Next.js + Medusa + MeiliSearch | 加载 -60%，搜索 +35% | 开源 Headless 差异化 |
| **Lush** | B2C 化妆品 | **Saleor** + Next.js | 全球多区域运营 | Saleor 企业级验证 |
| **MAAP** | B2C 骑行服饰 | **Saleor** + Next.js | 高性能全球站 | Saleor 性能验证 |

---

## 附录

### A. 关键工具与资源

| 类型 | 工具 | 费用 | 用途 |
|------|------|------|------|
| 电商引擎 | Saleor | 免费开源 | 产品/订单/客户/多区域 |
| 自定义后端 | FastAPI | 免费开源 | 询盘/RFQ/AI 服务 |
| 前端框架 | Next.js 15 | 免费开源 | SSR/ISR/SSG 混合渲染 |
| 前端托管 | Vercel | 免费层 | 全球边缘部署 |
| 竞品监控 | changedetection.io | 免费（自部署） | 竞品站价格/内容监控 |
| SEO 审计 | RustySEO | 免费开源 | 技术 SEO + 关键词 |
| 站点爬取 | LibreCrawl | 免费开源 | 全站结构分析 |
| 搜索引擎 | MeiliSearch | 免费（自部署） | 多语言语义搜索（阶段 3） |
| 热力图 | Microsoft Clarity | 免费 | 用户行为 + 录屏 |
| 邮件 | Brevo | 免费 300 封/天 | 邮件营销 + 自动化 |
| 分析 | GA4 | 免费 | 流量分析 |
| 站长工具 | Google Search Console | 免费 | 索引状态 + 搜索表现 |
| CDN/安全 | Cloudflare | 免费套餐 | CDN + SSL + WAF + DDoS |
| AI | DeepSeek API | ¥1-2/百万 token | AI 搜索/客服/内容 |
| 错误追踪 | Sentry | 免费层 | 错误监控 |
| CI/CD | GitHub Actions | 免费层 | 自动化部署 |

### B. Python 依赖清单

```
# fastapi/requirements.txt
fastapi==0.115.*
uvicorn[standard]==0.34.*
sqlalchemy==2.0.*
psycopg2-binary==2.9.*
alembic==1.14.*
celery[redis]==5.4.*
pydantic==2.10.*
pydantic-settings==2.7.*
httpx==0.28.*
playwright==1.49.*          # PDF 生成（替代 WeasyPrint）
brevo==1.0.*
openai==1.67.*
redis==5.2.*
python-multipart==0.0.*
gql[httpx]==3.5.*           # Saleor GraphQL 客户端
```

### C. 术语对照

| 英文 | 中文 | 说明 |
|------|------|------|
| Saleor | - | Python/Django 开源电商引擎 |
| Headless | 无头架构 | 前后端分离 |
| Channel | 渠道 | Saleor 多区域/多币概念 |
| GraphQL | - | API 查询语言 |
| FastAPI | - | Python 异步 Web 框架 |
| Celery | - | Python 异步任务队列 |
| ISR | 增量静态再生 | Next.js 缓存策略 |
| RSC | React Server Components | React 服务端组件 |

### D. v3.1 修订记录

| # | 修订项 | 原方案 | v3.1 方案 | 理由 |
|---|--------|--------|----------|------|
| 1 | FastAPI 数据库 | 共享 saleor_db | **独立 fastapi_db** | 避免 migrations 冲突，数据隔离 |
| 2 | 前端部署 | Docker 自建 nextjs 容器 | **Vercel 托管** | 海外边缘节点 SSR 性能最优 |
| 3 | Saleor 版本 | `3.20`（模糊） | **3.20.3（锁定）** | 避免 breaking change |
| 4 | CI/CD | 无 | **GitHub Actions** | 自动化部署，迭代效率 |
| 5 | 备份机制 | Sprint 3 才建立 | **Sprint 0b 建立** | 询盘数据是核心资产 |
| 6 | 服务器配置 | 4C8G | **8C16G 或拆分** | 8G 跑全部服务无余量 |
| 7 | 阶段 0 竞品分析 | 部署 3 个监控工具 | **零部署快速分析** | 中小企业降低初期复杂度 |
| 8 | MeiliSearch | 阶段 1 部署 | **阶段 3 引入** | MVP 50-100 产品不需要 |
| 9 | Dashboard 安全 | 公网无限制 | **IP 白名单** | 管理后台不应公网暴露 |
| 10 | GDPR 合规 | 仅 Cookie 横幅 | **完整合规清单** | 面向欧洲市场必须 |
| 11 | PDF 生成 | WeasyPrint | **Playwright** | WeasyPrint 在 alpine 中文字体支持差 |
| 12 | Celery 配置 | 加载 FastAPI 应用 | **独立 celery_config.py** | Worker 不加载路由，减少内存 |
| 13 | 量化里程碑 | 仅阶段 2 有 | **每阶段量化标准** | 提升计划可执行性 |
| 14 | Sprint 0 时长 | 2 周 | **3 周** | Saleor 部署配置复杂度 |
| 15 | 端口绑定 | 0.0.0.0 | **127.0.0.1** | 仅 Nginx 暴露公网 |
| 16 | Redis 密码 | 无 | **requirepass** | 生产环境必须 |
