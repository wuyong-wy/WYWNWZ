# WYWNWZ 外贸独立站 — 完整修复方案

> 基于《修改完善建议》+《CX项目检测报告》合并分析  
> 生成时间: 2026-05-27  
> 总计: 30 项修复，按 P0→P1→P2→P3 四级优先级排列

---

## P0 — 必须立即修复（阻碍上线）4 项

### Fix-01: Celery 任务在数据库事务提交前触发

| 属性 | 值 |
|------|-----|
| **严重级别** | CRITICAL |
| **文件** | `fastapi/app/api/inquiries.py:32-39`, `fastapi/app/database.py:27-35` |
| **问题** | `await db.flush()` 后立即调用 `send_inquiry_auto_reply.delay()`，但事务在 `get_db` 依赖退出后才 commit。Celery worker 可能在事务提交前查询数据库，导致找不到记录，**邮件静默丢失** |
| **影响** | 询盘提交成功但客户收不到自动回复邮件，管理员收不到通知 |

**修复方案：**

```python
# fastapi/app/api/inquiries.py — create_inquiry 函数
@router.post("/", response_model=dict, status_code=201)
async def create_inquiry(
    inquiry: InquiryCreate,
    db: AsyncSession = Depends(get_db),
):
    """提交询盘（公开端点，无需认证）"""
    db_inquiry = Inquiry(**inquiry.model_dump())
    db.add(db_inquiry)
    await db.commit()          # ← 先 commit，确保 Celery worker 能查到记录
    await db.refresh(db_inquiry)

    inquiry_id = str(db_inquiry.id)

    # 事务提交后再触发 Celery 任务
    send_inquiry_auto_reply.delay(inquiry_id)
    send_admin_notification.delay(inquiry_id)

    return {"success": True, "inquiry_id": inquiry_id}
```

> `get_db` 中的 `await session.commit()` 对已 commit 的 session 是空操作（幂等），无需修改。

---

### Fix-02: ADMIN_PASSWORD 生产环境无阻止级校验

| 属性 | 值 |
|------|-----|
| **严重级别** | HIGH |
| **文件** | `fastapi/app/config.py:61-66` |
| **问题** | 密码为空或过短仅打 `warning`，不阻止启动。部署时忘记设置密码会导致空密码可登录 |
| **影响** | 生产环境空密码可被登录，安全漏洞 |

**修复方案：**

```python
# fastapi/app/config.py — model_post_init 中
def model_post_init(self, __context) -> None:
    """启动校验：生产环境安全检查"""
    if not self.DEBUG:
        if self.ADMIN_JWT_SECRET == _DEFAULT_JWT_SECRET:
            raise ValueError(
                "ADMIN_JWT_SECRET must be changed from default value in production."
            )
        if not self.ADMIN_PASSWORD or len(self.ADMIN_PASSWORD) < 16:
            raise ValueError(                          # ← 从 warning 改为 ValueError
                "ADMIN_PASSWORD must be set with >= 16 characters in production. "
                "Set via ADMIN_PASSWORD environment variable."
            )
```

---

### Fix-03: Nginx 缺失 HSTS + CSP + SSL 加固

| 属性 | 值 |
|------|-----|
| **严重级别** | HIGH |
| **文件** | `nginx/nginx.conf:67-74` |
| **问题** | 无 `Strict-Transport-Security` 头、无 `Content-Security-Policy` 头、`ssl_ciphers` 未显式禁用弱算法、缺少 `ssl_ecdh_curve` |
| **影响** | 浏览器不会强制 HTTPS，可能遭受降级攻击；无 CSP 无法防止 XSS 数据外泄 |

**修复方案：**

```nginx
# nginx/nginx.conf — 在 443 server 块中修改/添加

# 1. 修改 ssl_ciphers，显式禁用弱算法
ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:!aNULL:!eNULL:!MD5:!3DES;
ssl_ecdh_curve X25519:secp384r1;

# 2. 添加 HSTS 头（在 Security headers 区域）
add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;

# 3. 添加 CSP 头
add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self'; connect-src 'self' https://api.yourdomain.com; frame-ancestors 'none';" always;

# 4. 添加 Permissions-Policy
add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
```

---

### Fix-04: 全局占位域名硬编码

| 属性 | 值 |
|------|-----|
| **严重级别** | HIGH |
| **文件** | 8+ 个前端文件 + `next-sitemap.config.js` |
| **问题** | `yourdomain.com` 硬编码在 15+ 处，部署时容易遗漏替换 |
| **影响** | 部署后 SEO canonical URL、Schema markup、sitemap 等指向错误域名 |

**修复方案：**

1. 创建 `frontend/lib/constants.ts`：

```typescript
export const SITE_URL =
  process.env.NEXT_PUBLIC_SITE_URL || "https://yourdomain.com";
export const SITE_NAME =
  process.env.NEXT_PUBLIC_SITE_NAME || "Foreign Trade";
export const CONTACT_EMAIL =
  process.env.NEXT_PUBLIC_CONTACT_EMAIL || "info@yourdomain.com";
export const CONTACT_PHONE =
  process.env.NEXT_PUBLIC_CONTACT_PHONE || "+86 138-0013-8000";
```

2. 全局替换所有硬编码域名为 `SITE_URL` 引用（涉及文件）：
   - `app/[locale]/layout.tsx` — metadata 中的 url/alternates
   - `app/[locale]/page.tsx` — Organization Schema
   - `app/[locale]/products/[slug]/page.tsx` — metadata + Product Schema
   - `app/[locale]/categories/[slug]/page.tsx` — metadata + Breadcrumb Schema
   - `components/shared/SchemaMarkup.tsx` — Schema 工厂函数
   - `components/layout/Footer.tsx` — 联系信息
   - `app/[locale]/contact/page.tsx` — 联系信息
   - `next-sitemap.config.js` — siteUrl

3. 在 `.env.example` 中添加：
```env
NEXT_PUBLIC_SITE_URL=https://yourdomain.com
NEXT_PUBLIC_SITE_NAME=Foreign Trade
NEXT_PUBLIC_CONTACT_EMAIL=info@yourdomain.com
NEXT_PUBLIC_CONTACT_PHONE=+86 138-0013-8000
```

---

## P1 — 上线前应完成 6 项

### Fix-05: 产品详情页 XSS 风险

| 属性 | 值 |
|------|-----|
| **严重级别** | HIGH |
| **文件** | `frontend/app/[locale]/products/[slug]/page.tsx:122` |
| **问题** | `dangerouslySetInnerHTML={{ __html: displayDescription }}` 渲染 Saleor 返回的 HTML，未做消毒 |

**修复方案：**

```bash
cd frontend && npm install dompurify && npm install -D @types/dompurify
```

```tsx
// products/[slug]/page.tsx
import DOMPurify from "dompurify";

// 替换
dangerouslySetInnerHTML={{ __html: DOMPurify.sanitize(displayDescription) }}
```

---

### Fix-06: 移除 config.py 硬编码默认 JWT 密钥

| 属性 | 值 |
|------|-----|
| **严重级别** | MEDIUM |
| **文件** | `fastapi/app/config.py:9,50` |
| **问题** | `_DEFAULT_JWT_SECRET` 硬编码在源码中，开发环境会静默使用可预测值 |

**修复方案：**

```python
# fastapi/app/config.py
# 删除 _DEFAULT_JWT_SECRET 常量

class Settings(BaseSettings):
    # ...
    ADMIN_JWT_SECRET: str = ""   # ← 改为空字符串

    def model_post_init(self, __context) -> None:
        if not self.ADMIN_JWT_SECRET or len(self.ADMIN_JWT_SECRET) < 32:
            if not self.DEBUG:
                raise ValueError(
                    "ADMIN_JWT_SECRET must be >= 32 chars in production."
                )
            else:
                logger.warning(
                    "admin_jwt_secret_insecure",
                    msg="Using insecure default JWT secret for development only.",
                )
                self.ADMIN_JWT_SECRET = "dev_insecure_jwt_secret_do_not_use_in_prod__"
```

---

### Fix-07: Redis 密码避免命令行暴露

| 属性 | 值 |
|------|-----|
| **严重级别** | MEDIUM |
| **文件** | `docker-compose.yml:139`, `scripts/healthcheck.sh:40` |
| **问题** | `redis-cli -a "${REDIS_PASSWORD}"` 在进程列表中暴露密码 |

**修复方案：**

```yaml
# docker-compose.yml — redis 服务
redis:
  environment:
    - REDISCLI_AUTH=${REDIS_PASSWORD}
  healthcheck:
    test: ["CMD", "redis-cli", "ping"]
    # REDISCLI_AUTH 环境变量会自动被 redis-cli 读取
```

```bash
# scripts/healthcheck.sh — Redis 检查部分
docker exec -e REDISCLI_AUTH="$REDIS_PASSWORD" \
  $(docker ps -qf name=redis 2>/dev/null) \
  redis-cli ping 2>/dev/null | grep -q PONG
```

---

### Fix-08: 健康检查端点增强

| 属性 | 值 |
|------|-----|
| **严重级别** | MEDIUM |
| **文件** | `fastapi/app/api/health.py` |
| **问题** | 仅返回 `{"status": "ok"}`，不检查 DB 和 Redis 连通性 |

**修复方案：**

```python
"""健康检查端点"""

from fastapi import APIRouter
from sqlalchemy import text

from app.config import settings
from app.database import async_session

router = APIRouter(tags=["health"])


@router.get("/health")
async def health_check():
    """健康检查 — 检测 DB 和 Redis 连通性"""
    checks = {"status": "ok", "database": "ok", "redis": "ok"}

    # 数据库检查
    try:
        async with async_session() as session:
            await session.execute(text("SELECT 1"))
    except Exception:
        checks["database"] = "unavailable"
        checks["status"] = "degraded"

    # Redis 检查
    try:
        import redis.asyncio as aioredis
        r = aioredis.from_url(settings.REDIS_URL)
        await r.ping()
        await r.close()
    except Exception:
        checks["redis"] = "unavailable"
        checks["status"] = "degraded"

    return checks
```

---

### Fix-09: 锁定关键依赖版本 + 修复 CVE

| 属性 | 值 |
|------|-----|
| **严重级别** | HIGH (CVE) + MEDIUM (版本锁定) |
| **文件** | `fastapi/requirements.txt`, `docker-compose.yml` |
| **问题** | `python-multipart==0.0.*` 存在 CVE-2024-24762；通配符版本导致构建不可重现；`saleor-dashboard:latest` 不安全 |

**修复方案：**

```
# fastapi/requirements.txt — 锁定精确版本
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
celery[redis]==5.4.0
pydantic==2.10.4
pydantic-settings==2.7.1
httpx==0.28.1
gql[httpx]==3.5.0
redis==5.2.1
python-multipart==0.0.9
sentry-sdk[fastapi]==2.19.2
structlog==24.5.0
PyJWT==2.9.0
```

```yaml
# docker-compose.yml — 锁定 Dashboard 版本
saleor-dashboard:
  image: ghcr.io/saleor/saleor-dashboard:3.20.3   # ← 从 latest 改为固定版本
```

---

### Fix-10: CORS allow_headers 收紧

| 属性 | 值 |
|------|-----|
| **严重级别** | MEDIUM |
| **文件** | `fastapi/app/main.py:55` |
| **问题** | `allow_headers=["*"]` 允许所有请求头 |

**修复方案：**

```python
# fastapi/app/main.py
app.add_middleware(
    CORSMiddleware,
    allow_origins=[settings.FRONTEND_URL],
    allow_credentials=True,
    allow_methods=["GET", "POST", "PATCH", "DELETE", "OPTIONS"],
    allow_headers=["Authorization", "Content-Type", "Accept"],
)
```

---

## P2 — 上线后应尽快完成 6 项

### Fix-11: Celery 任务改用同步数据库会话

| 属性 | 值 |
|------|-----|
| **严重级别** | MEDIUM |
| **文件** | `fastapi/app/tasks/email.py:58,99`, `fastapi/app/database.py` |
| **问题** | `asyncio.run()` 每次创建新事件循环，无法复用连接池；且 Celery worker 本身是同步进程 |

**修复方案：**

1. 在 `database.py` 中添加同步引擎：

```python
# fastapi/app/database.py — 追加同步引擎
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker, Session

_sync_url = settings.DATABASE_URL.replace("+asyncpg", "")
sync_engine = create_engine(_sync_url, pool_pre_ping=True, pool_size=5)
SyncSession = sessionmaker(sync_engine, class_=Session, expire_on_commit=False)
```

2. 重构 `tasks/email.py`：

```python
"""Celery 异步邮件任务"""

import structlog
from celery_config import celery_app
from sqlalchemy import select

from app.config import settings
from app.database import SyncSession
from app.models.inquiry import Inquiry
from app.services.email import brevo_send_sync  # 同步版 Brevo 发送

logger = structlog.get_logger()


def _get_inquiry(inquiry_id: str) -> Inquiry | None:
    """查询询盘记录（同步）"""
    with SyncSession() as db:
        result = db.execute(select(Inquiry).where(Inquiry.id == inquiry_id))
        return result.scalar_one_or_none()


@celery_app.task(queue="email", bind=True, max_retries=3, default_retry_delay=300)
def send_inquiry_auto_reply(self, inquiry_id: str):
    """自动回复询盘客户"""
    inquiry = _get_inquiry(inquiry_id)
    if not inquiry:
        logger.warning("inquiry_not_found", inquiry_id=inquiry_id)
        return

    template_id = 2 if inquiry.language == "zh" else 1
    success = brevo_send_sync(
        to_email=inquiry.customer_email,
        template_id=template_id,
        params={"customer_name": inquiry.customer_name, "product_name": inquiry.product_name or "your inquiry"},
    )
    if not success:
        raise self.retry(exc=Exception("Brevo send failed"))


@celery_app.task(queue="email", bind=True, max_retries=3, default_retry_delay=300)
def send_admin_notification(self, inquiry_id: str):
    """通知管理员有新询盘"""
    inquiry = _get_inquiry(inquiry_id)
    if not inquiry:
        logger.warning("inquiry_not_found", inquiry_id=inquiry_id)
        return

    success = brevo_send_sync(
        to_email=settings.ADMIN_EMAIL,
        template_id=3,
        params={
            "inquiry_id": str(inquiry.id),
            "customer_name": inquiry.customer_name,
            "customer_email": inquiry.customer_email,
            "product_name": inquiry.product_name or "N/A",
            "message": inquiry.message or "(no message)",
            "created_at": inquiry.created_at.isoformat(),
        },
    )
    if not success:
        raise self.retry(exc=Exception("Brevo send failed"))
```

---

### Fix-12: GraphQL 查询注入风险

| 属性 | 值 |
|------|-----|
| **严重级别** | MEDIUM |
| **文件** | `frontend/lib/saleor.ts:49,55,99,106,138,164,180` |
| **问题** | `langCode` 和 `channel` 通过模板字符串插值注入 GraphQL 查询 |

**修复方案：**

```typescript
// frontend/lib/saleor.ts — 添加白名单校验
const VALID_LANG_CODES = new Set([
  "EN", "ZH", "JA", "KO", "FR", "DE", "ES", "PT", "RU", "AR",
]);

function validateLangCode(code: string): string {
  const upper = code.toUpperCase();
  if (!VALID_LANG_CODES.has(upper)) {
    console.warn(`Invalid language code: ${code}, falling back to EN`);
    return "EN";
  }
  return upper;
}

// 在所有使用 langCode 的查询中调用
const safeLangCode = validateLangCode(langCode);
```

---

### Fix-13: 前端添加 ErrorBoundary

| 属性 | 值 |
|------|-----|
| **严重级别** | MEDIUM |
| **文件** | 新建 `frontend/components/shared/ErrorBoundary.tsx` |
| **问题** | 前端无错误边界，组件异常会导致白屏 |

**修复方案：**

```tsx
// frontend/components/shared/ErrorBoundary.tsx
"use client";
import { Component, type ReactNode } from "react";

interface Props { children: ReactNode; fallback?: ReactNode; }
interface State { hasError: boolean; }

export class ErrorBoundary extends Component<Props, State> {
  state: State = { hasError: false };

  static getDerivedStateFromError(): State {
    return { hasError: true };
  }

  componentDidCatch(error: Error, info: React.ErrorInfo) {
    console.error("ErrorBoundary caught:", error, info);
  }

  render() {
    if (this.state.hasError) {
      return this.props.fallback ?? (
        <div className="flex min-h-[50vh] items-center justify-center">
          <p className="text-sm text-gray-500">Something went wrong. Please refresh the page.</p>
        </div>
      );
    }
    return this.props.children;
  }
}
```

在 `app/[locale]/layout.tsx` 中用 `<ErrorBoundary>` 包裹页面内容。

---

### Fix-14: 前端硬编码英文文本 i18n 化缺

| 属性 | 值 |
|------|-----|
| **严重级别** | MEDIUM |
| **文件** | `ProductGrid.tsx:12`, `ProductGallery.tsx:18`, `not-found.tsx:7,11` |
| **问题** | "No products found"、"No images available"、"Page not found" 硬编码英文 |

**修复方案：**

1. 在 `messages/en.json` 和 `messages/zh.json` 中添加翻译键：

```json
// product 命名空间
{
  "product": {
    "noProducts": "No products found",
    "noImages": "No images available"
  },
  "common": {
    "notFound": "Page not found",
    "backToHome": "Back to Home"
  }
}
```

```json
// zh.json
{
  "product": {
    "noProducts": "暂无产品",
    "noImages": "暂无图片"
  },
  "common": {
    "notFound": "页面未找到",
    "backToHome": "返回首页"
  }
}
```

2. 在组件中使用 `useTranslations()` 替换硬编码文本。

---

### Fix-15: Nginx Dashboard IP 白名单占位符

| 属性 | 值 |
|------|-----|
| **严重级别** | MEDIUM |
| **文件** | `nginx/nginx.conf:90-91` |
| **问题** | `allow 203.0.113.0/24` 是 RFC 5737 保留地址，永远不会匹配真实流量 |

**修复方案：**

```nginx
# nginx/nginx.conf — Dashboard location
location /dashboard/ {
    # ⚠️ 替换为实际的办公/运维 IP 或 VPN 网段
    # 示例：allow 1.2.3.4/32;  # 办公室出口 IP
    # 示例：allow 10.200.0.0/16;  # VPN 网段
    allow 10.0.0.0/8;       # 内网（仅开发环境使用）
    deny all;
    # ...
}
```

> 部署时必须替换为真实 IP，否则 Dashboard 无法从外部访问。

---

### Fix-16: CI/CD 添加安全扫描

| 属性 | 值 |
|------|-----|
| **严重级别** | MEDIUM |
| **文件** | `.github/workflows/deploy.yml` |
| **问题** | 缺少依赖安全扫描步骤 |

**修复方案：**

在 `lint-and-test` job 中添加：

```yaml
- name: Security audit (pip)
  run: |
    pip install pip-audit
    pip-audit -r requirements.txt

- name: Type check (tsc)
  working-directory: frontend
  run: npx tsc --noEmit
```

在 `frontend-check` job 中添加：

```yaml
- name: Security audit (npm)
  run: npm audit --audit-level=high
```

---

## P3 — 优化建议（可延后）14 项

| # | 问题 | 文件 | 修复方案 |
|---|------|------|---------|
| 17 | `tasks/email.py` 未使用的 import/函数 | `fastapi/app/tasks/email.py:4,8,19-21` | 删除 `datetime`、`timezone`、`AsyncSession` 导入和 `_get_db_sync` 函数 |
| 18 | `schemas/inquiry.py` 未使用的 `HttpUrl` | `fastapi/app/schemas/inquiry.py:6` | 删除 `HttpUrl` 导入 |
| 19 | `InquiryButton.tsx` 无意义三元表达式 | `frontend/components/product/InquiryButton.tsx:21` | 简化为 `t("inquiryAbout")` |
| 20 | `CookieConsent` key 无版本管理 | `frontend/components/shared/CookieConsent.tsx:7` | 改为 `cookie_consent_v2` |
| 21 | `about/privacy` 页面内容硬编码 | `frontend/app/[locale]/about/page.tsx`, `privacy/page.tsx` | 将内容移至翻译文件或 CMS |
| 22 | `saleor.ts` `t()` 和 `tCategory()` 重复 | `frontend/lib/saleor.ts:265-281` | 使用泛型合并为单一函数 |
| 23 | `auth.py` Redis 客户端线程安全 | `fastapi/app/api/auth.py:20-27` | 使用 `functools.lru_cache` 或添加锁 |
| 24 | `saleor.py` 启动时创建客户端 | `fastapi/app/services/saleor.py:28-29` | 改为懒加载模式 |
| 25 | `alembic/env.py` 通配符 import | `fastapi/alembic/env.py:14` | 改为显式 `from app.models.inquiry import Inquiry` |
| 26 | 缺少 `package-lock.json` | `frontend/` | 运行 `npm install` 生成并提交 |
| 27 | 缺少 `.editorconfig` | 项目根目录 | 创建统一缩进/换行符配置 |
| 28 | 前端 `fetch` 缺少超时处理 | `frontend/lib/api.ts` | 添加 `AbortController` 15s 超时 |
| 29 | Header 语言切换缺少 click-outside | `frontend/components/layout/Header.tsx` | 添加 `useClickOutside` hook |
| 30 | `next.config.ts` `serverActions` 移出 experimental | `frontend/next.config.ts` | Next.js 15 已稳定，移到顶层配置 |

---

## 修复执行路线图

```
第 1 天 — P0 阻塞上线（4 项）
  ├── Fix-01  Celery 事务提交顺序修复        ← 最关键，影响核心业务
  ├── Fix-02  ADMIN_PASSWORD 阻止级校验      ← 安全漏洞
  ├── Fix-03  Nginx HSTS + CSP + SSL 加固    ← 安全合规
  └── Fix-04  全局占位域名替换为环境变量      ← 部署必需

第 2-3 天 — P1 上线前加固（6 项）
  ├── Fix-05  产品详情页 XSS 消毒            ← 安全漏洞
  ├── Fix-06  JWT 密钥硬编码移除
  ├── Fix-07  Redis 密码暴露修复
  ├── Fix-08  健康检查增强
  ├── Fix-09  依赖版本锁定 + CVE 修复
  └── Fix-10  CORS headers 收紧

第 1 周 — P2 上线后完善（6 项）
  ├── Fix-11  Celery 同步会话改造
  ├── Fix-12  GraphQL 查询注入防护
  ├── Fix-13  前端 ErrorBoundary
  ├── Fix-14  前端 i18n 化缺修复
  ├── Fix-15  Nginx Dashboard IP 白名单
  └── Fix-16  CI/CD 安全扫描

第 2 周起 — P3 持续优化（14 项）
  └── Fix-17~30 逐项推进
```

---

## 修复影响矩阵

| 修复项 | 涉及文件数 | 是否需要重启服务 | 是否需要数据库迁移 | 是否影响 API 接口 |
|--------|-----------|----------------|-------------------|------------------|
| Fix-01 | 1 | 是（FastAPI） | 否 | 否 |
| Fix-02 | 1 | 是（FastAPI） | 否 | 否 |
| Fix-03 | 1 | 是（Nginx reload） | 否 | 否 |
| Fix-04 | 8+ | 否（前端重新部署） | 否 | 否 |
| Fix-05 | 1 | 否（前端重新部署） | 否 | 否 |
| Fix-06 | 1 | 是（FastAPI） | 否 | 否 |
| Fix-07 | 2 | 是（Redis + Nginx） | 否 | 否 |
| Fix-08 | 1 | 是（FastAPI） | 否 | 是（health 响应格式变更） |
| Fix-09 | 2 | 是（重建镜像） | 否 | 否 |
| Fix-10 | 1 | 是（FastAPI） | 否 | 否 |
| Fix-11 | 2 | 是（FastAPI + Celery） | 否 | 否 |
| Fix-12 | 1 | 否（前端重新部署） | 否 | 否 |
| Fix-13 | 2 | 否（前端重新部署） | 否 | 否 |
| Fix-14 | 5+ | 否（前端重新部署） | 否 | 否 |
| Fix-15 | 1 | 是（Nginx reload） | 否 | 否 |
| Fix-16 | 1 | 否（CI/CD 仅） | 否 | 否 |

---

## 综合评分

| 维度 | 当前评分 | P0+P1 修复后 | 全部修复后 |
|------|---------|-------------|-----------|
| 架构设计 | 3.8 | 4.0 | 4.2 |
| 代码质量 | 3.8 | 4.0 | 4.5 |
| 安全性 | 3.5 | 4.2 | 4.5 |
| 测试覆盖 | 2.5 | 2.5 | 3.5 |
| 部署运维 | 3.5 | 4.0 | 4.5 |
| SEO | 4.5 | 4.5 | 4.5 |
| **综合** | **3.6** | **3.9** | **4.3** |
